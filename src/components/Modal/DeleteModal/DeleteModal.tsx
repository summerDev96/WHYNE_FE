import BasicModal from '@/components/common/Modal/BasicModal';

export default function DeleteModal() {
  return (
    <div>
      <BasicModal type='register' title='모달 제목' open={true} onOpenChange={} />
    </div>
  );
}
